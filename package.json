{
  "name": "svg-canvas",
  "version": "0.8.1",
  "description": "'svg-canvas' is a TypeScript package used to simplify the drawing and animation of SVG elements on the web using fluent interfaces and 'frame-by-frame' animation.",
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage"
  },
  "author": "Ewan James Donovan",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.19",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.3",
    "browserify": "^17.0.0",
    "tsify": "^5.0.2"
  }
}
